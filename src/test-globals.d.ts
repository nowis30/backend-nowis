/// <reference types="jest" />
// Provides Jest global types (describe, it, expect, jest, beforeAll, afterAll, etc.)
// to the TypeScript language server so test files stop showing missing name errors
// in editors and tooling that don't use the dedicated test tsconfig.
